import { AttractDirective } from './attract.directive';

describe('AttractDirective', () => {
  it('should create an instance', () => {
    const directive = new AttractDirective();
    expect(directive).toBeTruthy();
  });
});
